from __future__ import annotations

DEFAULT_TOP10_PROMPTS = {
    "master": """PROMPT_0_MASTER_TOP10: Оркестратор «Структура по топ-10» (3 вкладки, таблица опционально)

Внутренняя роль (не выводи): руководитель UX-аналитики, лауреат Webby.

Приоритет:
Этот промпт главный. Остальные промпты использовать только как внутренние инструкции в нужных шагах.

Вход:
- run_id, run_datetime
- queries[]
- pages[]: для каждой страницы:
  - site
  - page_url
  - device (desktop|mobile)
  - screenshot_refs[]
  - text_full
  - page_type (service|portal_article) если уже определено, иначе модель определяет
  - page_type_confidence (0–1) если page_type определялся моделью

Цель:
Сформировать текстовый отчёт в 3 вкладках:
1) «Общие выводы»
2) «Нормализованные блоки»
3) «Предложение по структуре оптимальной страницы»
Плюс подготовить внутренние структурированные данные `structures_rows[]` (источник правды для расчётов), но НЕ экспортировать таблицу автоматически.

Общие правила:
- Приоритет: скриншоты. Текст подряд использовать для заголовков/уточнений, очищая меню/футер/повторы.
- Не оценивать визуальную «красоту».
- Не выдумывать факты/условия. Если нет — «не указано».
- Словарь блоков держать коротким: 15–25 канонических block_name.
- CTA не выделять отдельным блоком: только атрибуты cta_present, cta_type.
- proof_type всегда атрибут блока: none|numbers|ratings|cases|clients|experts.
- confidence: 0–1 по каждому блоку.
- Во вкладках не использовать таблицы; только списки и подзаголовки.
- Частотности: X/N и %.
- Качество распознавания: доля блоков с confidence ≥ 0,7.
- Нейминг блоков:
  - системные идентификаторы только на EN snake_case: l1_id, l2_id, l3_id;
  - человекочитаемые подписи только на русском: l1_label_ru, l2_label_ru, l3_label_ru.
- В отчёте не используй сухое «L1/L2/L3» без пояснений.
  Используй формат:
  «категория блока (l1_id) / тип блока (l2_id) / подтип блока (l3_id)».

Порядок работы:
Шаг 1. Выполни PROMPT_1_BLOCKS_CORE и получи:
- вкладку «Нормализованные блоки»
- structures_rows[] (внутренние данные по блокам)

Шаг 2. Выполни PROMPT_2_SUMMARY_NORMS на базе structures_rows[] и собери вкладку «Общие выводы».

Шаг 3. Выполни PROMPT_3_PROPOSED_STRUCTURE_CONSTRUCTOR на базе structures_rows[] и собери вкладку «Предложение по структуре…».

Формат финального ответа (строго):
ВКЛАДКА 1. «Общие выводы»
...
ВКЛАДКА 2. «Нормализованные блоки»
...
ВКЛАДКА 3. «Предложение по структуре оптимальной страницы»
...

СЛУЖЕБНЫЕ ДАННЫЕ (для экспорта по желанию)
Обязательный формат:
structures_rows:
```json
[ ... ]
```

Начинай сразу, вопросов не задавай.
""",
    "blocks_core": """PROMPT_1_BLOCKS_CORE: Разметка структуры top-10 (вкладка «Нормализованные блоки» + structures_rows)

Внутренняя роль (не выводи): UX-аналитик структуры, лауреат Red Dot.

Цель:
1) Определить page_type для каждой страницы, если не задано: service или portal_article.
2) Сформировать короткий справочник блоков (15–25 канонических block_name).
3) Разметить каждую страницу блоками сверху вниз.
4) Подготовить внутренний массив structures_rows[] (по одной строке на блок на странице).

Ограничения:
- Не анализировать смыслы/УТП/офферы.
- Не предлагать оптимальную структуру.
- CTA не выделять отдельным блоком.

Атрибуты блока (обязательные):
- site
- page_url
- page_type (service|portal_article)
- block_index (1…N)
- l1_id
- l1_label_ru
- l2_id
- l2_label_ru
- l3_id (nullable)
- l3_label_ru (nullable)
- block_name (канонический нормализованный блок)
- section (Hero|AboveFold|Mid|Bottom|Footer)
- heading_text (если найден, иначе null)
- cta_present (0/1)
- cta_type (button|form|link|multiple|null)
- proof_type (none|numbers|ratings|cases|clients|experts)
- confidence (0–1)
- notes (1 строка: что именно внутри)
Опционально:
- device
- screenshot_ref (1 основной ref или null)

Правила определения page_type:
- service: продаёт услугу/решение, есть захват/формы/этапы/кейсы/условия.
- portal_article: статья/гид/обзор/обучающий материал, доминирует контент, оглавление, автор, дата, блоки чтения.
Если не уверен — ставь page_type_confidence < 0,7 и поясни в notes первой строки.

Правила нормализации block_name:
- Словарь 15–25 блоков, без раздутия.
- Синонимы сводить к одному имени.
- CTA — атрибут, не блок.
- proof_type — атрибут: где есть доказательство доверия (цифры/рейтинги/кейсы/клиенты/эксперты).
- Для расчётов и матрицы authoritative ключ блока — l2_id.
- block_name оставляй только для обратной совместимости (deprecated).

Выход (строго, 2 части):

ЧАСТЬ A. Вкладка «Нормализованные блоки»
0) Краткий вывод по структуре (5–7 пунктов, с X/N и %):
- сколько страниц service и portal_article
- качество распознавания: доля блоков с confidence ≥ 0,7 (в целом и по типам)
- 5–7 самых частых блоков для service и для portal_article

1) Короткий справочник блоков (15–25 карточек):
категория блока (l1_id) / тип блока (l2_id) / подтип блока (l3_id) → роль (маршрутизация/доверие/оффер/объяснение) → как распознать → типовые вариации

2) По каждой странице (кратко):
- page_type + (если определялось моделью) page_type_confidence
- список блоков сверху вниз: block_index, категория блока (l1_id), тип блока (l2_id), подтип блока (l3_id), section, cta_present, proof_type, confidence

ЧАСТЬ B. СЛУЖЕБНЫЕ ДАННЫЕ
Обязательный формат:
structures_rows:
```json
[JSON массив объектов строго по полям выше]
```

Начинай сразу после получения входных данных.
""",
    "summary_norms": """PROMPT_2_SUMMARY_NORMS: Вкладка «Общие выводы» для top-10 (по structures_rows)

Внутренняя роль (не выводи): аналитик конкурентных паттернов.

Вход:
- run_id, run_datetime
- queries[]
- pages[] (site, page_url, device)
- structures_rows[] (из PROMPT_1_BLOCKS_CORE)

Цель:
Собрать вкладку «Общие выводы» строго по требованиям.

Правила расчётов:
- Качество распознавания: доля строк, где confidence ≥ 0,7.
- Нормы структуры считать отдельно для service и portal_article.
- Для «типового маршрута блоков» использовать только строки с confidence ≥ 0,7.
- «Типовой маршрут» = 8–12 шагов, порядок по медианной block_index.
- Must-have = block_name с частотой ≥ 70% в рамках page_type.
- Nice-to-have = 30–70% в рамках page_type.
- Частотности считать по l2_id, в выводе показывать формат: <l2_label_ru> (<l2_id>).
- Дифференциаторы: наблюдаемые признаки, формат «признак → X/N и %».
- Антипаттерны: «признак → риск → X/N и %».

Выход вкладки «Общие выводы» (строго):
0) Краткий вывод (5–7 пунктов)

1) Паспорт
- Сайты, страницы (URL)
- Дата, run_id
- Типы страниц: сколько service, сколько portal_article
- Режим: desktop/mobile
- Качество распознавания: % confidence ≥ 0,7 (в целом и по типам)

2) Нормы структуры по каждому типу страницы
2.1 service
- типовой маршрут блоков (8–12 шагов)
- must-have
- nice-to-have
2.2 portal_article
- типовой маршрут блоков (8–12 шагов)
- must-have
- nice-to-have

3) Дифференциаторы (что отличает сильных)
- 3–7 пунктов: «признак → X/N и % → почему важно (1 строка)»

4) Антипаттерны
- 3–7 пунктов: «признак → риск → X/N и %»

5) Итог
- «Оптимальная структура страницы под услугу» (список блоков по порядку)
- 3 варианта архетипов, только если реально видны разные кластеры:
  Для каждого: 8–12 шагов и чем отличается (2–3 признака).

Начинай сразу.
""",
    "proposed_structure": """PROMPT_3_PROPOSED_STRUCTURE_CONSTRUCTOR: Вкладка «Предложение по структуре оптимальной страницы» (конструктор)

Внутренняя роль (не выводи): UX-архитектор, лауреат Webby.

Вход:
- structures_rows[] (из PROMPT_1_BLOCKS_CORE)

Главное правило:
Используй только строки, где page_type=service и confidence ≥ 0,7.

Цель:
Собрать предложенную оптимальную структуру в формате конструктора:
1) Скелет (L1-линии)
2) Внутри каждой линии: 2–5 блоков (L2 = block_name)
3) Для каждого блока: цель, что внутри, CTA, proof_type

L1-линии (фиксированные):
- Навигация
- Оффер/захват
- Объяснение/что делаем
- Маршрутизация (услуги/решения/варианты)
- Доверие
- Кейсы
- Процесс
- Условия
- FAQ/снятие рисков
- Финальный захват и контакты

Правила сборки:
- Маршрут 8–12 шагов: порядок по медианной позиции block_index.
- Не плодить блоки: использовать block_name из входных данных.
- Не плодить блоки: использовать l2_id из входных данных (block_name только fallback).
- CTA как атрибут: где CTA должен быть и какого типа.
- proof_type указывать там, где нужен «якорь доверия».
- Контроль «перегрева захвата»: 3–5 точек CTA на страницу, если в выборке есть перегрев — явно отметить.

Выход вкладки (строго):
0) Краткий вывод по предложенной структуре (5–7 пунктов)

1) Маршрут (8–12 шагов)
- шаг 1…N: block_name + section + почему здесь (частота/позиция)
- шаг 1…N: тип блока (l2_label_ru + l2_id) + section + почему здесь (частота/позиция)

2) Конструктор по L1 линиям
Для каждой L1 линии:
- 2–5 блоков (block_name). Для каждого:
- 2–5 блоков (l2_id). Для каждого:
  - Цель (1 строка)
  - Что внутри (3–6 пунктов)
  - CTA (cta_present? да/нет, cta_type)
  - Proof (proof_type)
  - Типовые вариации (1–2)

3) Контроль распределения CTA
- типичное число CTA-блоков по выборке
- рекомендуемое распределение по шагам (3–5 точек)

Начинай сразу.
""",
    "export_optional": """PROMPT_4_EXPORT_TABLE_OPTIONAL: Экспорт таблицы по желанию (Sheet A + Sheet B)

Внутренняя роль (не выводи): аналитик данных.

Вход:
- structures_rows[] (из PROMPT_1_BLOCKS_CORE)
- proposed_structure_text

Цель:
Подготовить 2 листа для таблицы:
Sheet A = сравнительный анализ нормализованных блоков по сайтам (X/N и комментарии).
Sheet B = предложение по оптимальной структуре с комментариями по блокам.

Правила:
- Sheet B строится только на основе предложенной структуры и подтверждённых блоков из structures_rows[].
- Канонические block_name должны совпадать строго (без синонимов).
- Канонические l2_id должны совпадать строго (без синонимов).
- В fallback-тексте формат блока: <l2_label_ru> (<l2_id>).
- Без таблиц и markdown-таблиц, только списки.
- Не добавляй вступления/заключения вне двух секций.

Формат вывода (строго):
### SHEET_1_COMPARE
1. <l2_label_ru> (<l2_id>) — сайты: <site1>, <site2>, ... — встречаемость: X/N (%) — комментарий: <кратко>
...

### SHEET_2_PROPOSAL
1. <l2_label_ru> (<l2_id>) — <комментарий по цели/месту/наполнению>
...
""",
}

DEFAULT_TOP10_PROMPT_ENABLED = {
    "master": True,
    "blocks_core": True,
    "summary_norms": True,
    "proposed_structure": True,
    "export_optional": True,
}
